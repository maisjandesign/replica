{
  "manifest_version": 3,
  "name": "Replica — Capture UI into Code",
  "version": "0.7.2",
  "description": "Capture any website section and turn its UI, motion, and visual context into implementation-ready code instructions.",
  "permissions": ["activeTab", "scripting", "storage"],
  "action": {
    "default_title": "Open Replica"
  },
  "background": {
    "service_worker": "background.js"
  },
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["content.js"],
      "run_at": "document_idle"
    }
  ]
}
